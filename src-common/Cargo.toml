[package]
name = "friendolls-common"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
specta = { version = "2.0.0-rc.21", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
